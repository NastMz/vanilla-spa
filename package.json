{
  "name": "vanilla-spa",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "test": "node --test --import ./scripts/register-test-hooks.mjs",
    "test:watch": "node --test --watch --import ./scripts/register-test-hooks.mjs",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "jsdom": "^26.1.0",
    "vite": "^8.0.0-beta.13"
  },
  "pnpm": {
    "overrides": {
      "vite": "^8.0.0-beta.13"
    }
  }
}
